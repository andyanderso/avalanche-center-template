# danger_rose

Byte-for-byte identical between Gulmarg and Argentina (verified via
`diff -rq`, see `AVALANCHE_CENTER_DISTRIBUTION_PLAN.md` §2). Copy as-is,
no genericizing needed.

Documented as "copy as-is" since Phase 1, but the actual copy never
happened until Phase 7's greenfield install test failed on a missing
required module and caught it. See plan §20 (Phase 7 log).
