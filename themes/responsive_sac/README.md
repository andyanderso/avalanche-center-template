# responsive_sac

Shared as-is; only `logo.png`, `responsive_sac_sponsors.css`, and
`responsive_sac.info` diverge between sites (pure branding). Driven by theme
settings instead of committed per-center assets. Implemented in Phase 2.
See `AVALANCHE_CENTER_DISTRIBUTION_PLAN.md` §8.
